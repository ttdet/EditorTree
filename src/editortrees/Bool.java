package editortrees;

/**
 * A booleaner container class used in methods involving verification (no adjustments).
 */
public class Bool {
    public boolean bool;
    public Bool(boolean bool) {
        this.bool = bool;
    }
}
